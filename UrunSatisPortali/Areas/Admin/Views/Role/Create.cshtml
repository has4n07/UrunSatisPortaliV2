@model UrunSatisPortali.ViewModels.RoleViewModel

@{
    ViewData["Title"] = "Yeni Rol Oluştur";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card border-0 shadow-lg mt-4">
            <div class="card-header bg-dark text-white py-3">
                <h5 class="mb-0 text-white font-playfair fs-5"><i class="bi bi-plus-circle me-2"></i>Yeni Rol Oluştur
                </h5>
            </div>
            <div class="card-body p-4">
                <form asp-action="Create" method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                    <div class="mb-3">
                        <label asp-for="Name" class="form-label fw-bold">Rol Adı</label>
                        <input asp-for="Name" class="form-control rounded-0" placeholder="Örn: Manager" />
                        <span asp-validation-for="Name" class="text-danger small"></span>
                    </div>

                    <div class="d-grid gap-2 d-md-flex justify-content-md-end mt-4">
                        <a asp-action="Index" class="btn btn-outline-secondary rounded-0 px-4">İptal</a>
                        <button type="submit" class="btn btn-dark rounded-0 px-4">Oluştur</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
